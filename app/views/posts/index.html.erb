<h3>Welcome!</h3>

<ul>
  <% @posts.each do |post| %>
    <li>
      <%=link_to '+', vote_post_path(post,{vote:true}), method:'post' %> / <%=post.total_votes%> / <%=link_to '-', vote_post_path(post,{vote:false}), method:'post' %>  <%= link_to post.title, post %>
    </li>
  <%end%>
</ul>
